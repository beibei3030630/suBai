<!--component/banner/index.wxml-->
<swiper class="banner" indicator-dots="{{dots}}" autoplay="{{autoplay}}" interval="{{interval}}">
    <block wx:for="{{imgUrl}}">
      <swiper-item>
        <image src="{{item}}"></image>
      </swiper-item>
    </block>
  </swiper>
